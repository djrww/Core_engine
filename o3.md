# Core_engine Rust 程式碼庫檢視報告

**儲存庫：** `github.com/djrww/Core_engine`  
**檢視版本：** 2026 年 9 月 4 日的 HEAD

---

## 1. 整體架構與優點

- 專案採用單一工作區風格的 crate 結構，並透過 `src/lib.rs` 重新匯出約 70 個公開模組。
- 詞法分析器、語法分析器、Span 工具及測試套件中的程式碼整體符合 Rust 慣用寫法，並以安全 Rust 為主。全儲存庫搜尋未發現 `unsafe` 區塊。
- 程式碼透過 Rustdoc 清楚記錄部分核心不變條件，例如：
  - 詞法分析器的完整覆蓋（tiling）特性；
  - 語法樹的連續性公理。
- `tests/` 目錄內包含約 1,400 行的屬性測試，並透過 CI 產生測試覆蓋率報告，以驗證上述不變條件。
- 語法分析器使用明確的遞迴計數器及固定上限：

  ```rust
  RECURSION_LIMIT = 256
  ```

  這比完全依賴系統呼叫堆疊更適合處理模糊測試及非可信輸入。

- `TokKind`、`Kind`、`Span` 等結構及列舉體積小，並可複製，適合在大型向量及多個處理階段中低成本傳遞。
- 程式碼整體遵循現代 Rust 慣例，例如：
  - 對外 API 中沒有大量使用可能直接 panic 的 `unwrap()`；
  - 廣泛使用 `Result`；
  - 合理使用 `Debug`、`Clone` 等 derive；
  - README 中包含 Clippy 品質檢查要求。

---

## 2. 具體錯誤與風險

### B-1：`Span::shift` 存在整數溢位及索引回繞風險

`Span::shift` 使用未經檢查的型別轉換計算新邊界：

```rust
start: (self.start as i64 + delta) as u32,
end:   (self.end   as i64 + delta) as u32,
```

可能出現以下問題：

- 如果傳入負數 `delta`，並將區間移動至 0 之前，轉換為 `u32` 時可能回繞；
- 如果傳入正數 `delta`，使結果超出 `u32::MAX`，同樣可能產生截斷或回繞；
- 最終可能建立無效的 `Span`；
- 後續所有依賴位元組索引及 Span 邊界的邏輯都可能受到破壞。

#### 建議修正

將方法改為回傳受檢查的結果：

```rust
fn checked_shift(self, delta: i64) -> Option<Span>
```

或者：

```rust
fn checked_shift(self, delta: i64) -> Result<Span, SpanOverflow>
```

也可以考慮：

- 內部改用 `u64` 索引；
- 使用 `checked_add`／`checked_sub`；
- 如果產品語意允許，再使用 `saturating_add`／`saturating_sub`。

不建議單純依賴 `as` 進行縮窄轉型。

---

### B-2：`Span::new` 只在 Debug 模式驗證邊界

目前 `Span::new` 使用：

```rust
debug_assert!(start <= end);
```

`debug_assert!` 在 Release 建置中通常會被移除，因此正式環境仍可能建立：

```text
start > end
```

的無效 Span。

#### 建議修正

可採用以下其中一種方式：

1. 使用 `assert!`，在所有建置模式下強制驗證；
2. 將建構函式改為受檢查 API：

   ```rust
   fn new(start: u32, end: u32) -> Option<Self>
   ```

3. 提供明確的錯誤類型：

   ```rust
   fn try_new(start: u32, end: u32) -> Result<Self, InvalidSpan>
   ```

4. 限制欄位可見性，避免外部程式碼直接建立無效值。

---

### B-3：詞法分析器可能切分 UTF-8 字元

目前 DFA 以原始位元組為單位移動。

如果遇到 UTF-8 延續位元組，例如：

```text
0x80–0xBF
```

系統可能將其視為單獨的 `Bad` token，並一次只消耗一個位元組。

這會導致：

- 一個多位元組 UTF-8 字元被拆成多個 token；
- token 邊界可能不再落在有效 UTF-8 字元邊界上；
- 後續使用字串切片時可能發生 panic；
- 診斷資訊可能指向半個字元；
- 詞法分析器宣告的不變條件可能在「字元層級」上失效。

需要注意的是，如果 tiling invariant 僅表示「每個來源位元組都恰好屬於一個 token」，逐位元組產生 `Bad` token 未必會破壞位元組層級的完整覆蓋；真正風險在於 UTF-8 字元邊界及後續字串切片安全性。

#### 建議修正

可考慮以下方案：

- 改以 `char_indices()` 處理 Unicode；
- 保留 ASCII 快速路徑，但對非 ASCII 位元組完整辨識 UTF-8 序列；
- 將輸入明確定義為任意位元組流，並避免後續以 `str[start..end]` 直接切片；
- 增加 UTF-8、無效 UTF-8、組合字元及 emoji 的測試案例。

---

### B-4：仍可能出現深層呼叫堆疊

雖然語法分析器具備遞迴深度計數器，但實際解析函式仍然彼此遞迴，例如：

```text
parse_expr
  → parse_unary
    → parse_expr
      → ...
```

惡意輸入可透過大量巢狀運算式或一元運算子建立深層函式呼叫。

即使 `RECURSION_LIMIT = 256` 能限制遞迴深度，系統仍可能：

- 建立多達約 256 層的呼叫框架；
- 在小堆疊執行緒或模糊測試環境中增加堆疊壓力；
- 因不同解析路徑而出現限制不一致；
- 在錯誤恢復時重複進入深層解析流程。

#### 建議修正

- 將可線性解析的運算式改寫成迴圈；
- 對一元運算子鏈使用迭代方式收集；
- 對括號及其他巢狀結構使用顯式堆疊；
- 確保進入任何遞迴路徑前都先進行深度檢查；
- 增加超過 256、1,000、10,000 層巢狀輸入的壓力測試。

---

### B-5：`Node::children` 可能出現無限制的 `Vec` 成長

子節點使用：

```rust
Vec<NodeId>
```

儲存。

對於大量 trivia、錯誤 token 或特殊病態輸入，`attach_leaf` 等流程可能反覆建立或擴展中間向量，帶來：

- 大量記憶體配置；
- 不必要的重新配置及複製；
- 極端情況下可能接近 O(N²) 的處理成本；
- 受到非可信輸入觸發時，可能形成拒絕服務風險。

#### 建議修正

可以評估使用：

```rust
SmallVec<[NodeId; 4]>
```

因為多數 AST 節點的直接子節點數量通常較少。

但不應預先假設一定能提升 30% 效能；應先透過 benchmark 和記憶體剖析確認：

- 平均子節點數；
- 配置次數；
- 峰值記憶體；
- 解析延遲；
- 大型及病態輸入下的複雜度。

---

## 3. 技術債熱點

### TD-1：存在尚未完成的實作模組

`lib.rs` 匯出了多個公開模組，其中部分檔案為空、只包含初步架構，或存在 `todo!()`，例如：

- `maude_engine.rs`
- `rocq_export.rs`

如果未完成 API 已被公開匯出，可能導致：

- 下游使用者誤以為功能穩定；
- 後續修改造成破壞性 API 變更；
- 執行到 `todo!()` 時直接 panic；
- 文件及實際功能不一致。

#### 建議修正

將未完成模組放在實驗性 feature flag 後：

```rust
#[cfg(feature = "experimental")]
pub mod maude_engine;
```

也可以進一步：

- 標記 `#[doc(hidden)]`；
- 使用 `#[deprecated]` 提醒尚未穩定；
- 在 README 中列出模組成熟度；
- 在穩定版本中避免匯出未完成 API。

---

### TD-2：單一 crate 可能增加編譯及維護成本

目前約 70 個公開模組集中在同一個 crate 中，可能帶來：

- 小幅修改觸發較大範圍的重新編譯；
- 模組間耦合逐步提高；
- 公開 API 邊界不清晰；
- 測試無法按子系統快速執行；
- 功能旗標管理變得複雜。

#### 建議拆分

可考慮拆成：

```text
core-ast
core-lexer
core-parser
core-proof
core-export
core-tooling
core-cli
```

拆分前應先利用：

```bash
cargo build --timings
```

取得實際編譯數據，避免在缺乏測量的情況下過早拆分。

---

### TD-3：錯誤類型分散

目前不同模組可能各自定義錯誤表達方式，例如：

- `ParseIssue`
- `Bad` token
- 樹結構相關錯誤列舉
- 其他模組自己的錯誤型別

這會增加：

- 錯誤轉換樣板程式碼；
- API 使用難度；
- CLI 或上層應用統一顯示錯誤的成本；
- 保留來源錯誤與 Span 資訊的難度。

#### 建議修正

建立分層錯誤模型，例如：

```rust
pub enum EngineError {
    Lex(LexError),
    Parse(ParseError),
    Tree(TreeError),
    Export(ExportError),
}
```

可以搭配 `thiserror` 自動產生：

- `Display`
- `Error`
- `From`

但應保留各子系統的具體錯誤類型，不建議把所有情況壓縮成只有字串的單一錯誤。

---

### TD-4：缺少標準化的模糊測試目標

README 中提及 `fuzz_daemon`，但儲存庫中缺少清晰、獨立的 `fuzz/` 測試架構及 corpus 管理方式。

#### 建議修正

整合 `cargo-fuzz`，至少建立以下目標：

```text
fuzz_lexer
fuzz_parser
fuzz_span
fuzz_roundtrip
fuzz_error_recovery
```

並在 CI 中：

- 執行短時間 smoke fuzz；
- 定期執行較長時間 nightly fuzz；
- 保存 crash corpus；
- 對修復過的錯誤增加回歸測試；
- 限制單次輸入的 CPU、記憶體及執行時間。

---

### TD-5：靜態分析閘門不足

README 雖建議執行：

```bash
cargo clippy -- -D warnings
```

但仍應確認 CI 是否真正強制執行，而不是僅存在於文件中。

#### 建議加入 CI

```bash
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test --all-features
cargo audit
cargo deny check
```

`clippy::pedantic` 不建議直接對整個專案無條件設為錯誤，因為可能產生大量低價值警告。較適合的方式是：

- 逐步啟用；
- 針對重要 lint 設為 `deny`；
- 對合理例外加上具體說明。

---

## 4. 安全性評估

### 正面因素

- 未發現 `unsafe` 程式碼；
- Rust 編譯器可提供基本記憶體安全保證；
- 主要程式碼未顯示大量網路存取或任意路徑檔案操作；
- 對外解析 API 的主要風險偏向：
  - panic；
  - 堆疊耗盡；
  - 記憶體耗盡；
  - CPU 資源耗盡；
  - 拒絕服務。

### 需要關注的攻擊面

如果詞法或語法分析器接受非可信輸入，應特別防範：

1. 超長輸入；
2. 極深巢狀結構；
3. 大量錯誤 token；
4. 無效 UTF-8；
5. 巨量 trivia；
6. 反覆觸發錯誤恢復；
7. 可導致大量節點配置的輸入；
8. Span 回繞或無效索引。

### 強化建議

在 Release 測試設定中開啟溢位檢查：

```bash
RUSTFLAGS="-C overflow-checks=on" cargo test --release
```

同時在 CI 中加入：

```bash
cargo audit
cargo deny check
```

並考慮定期執行：

- Miri；
- AddressSanitizer；
- LeakSanitizer；
- 模糊測試；
- 任意位元組輸入測試；
- 大型輸入資源限制測試。

需要注意：沒有 `unsafe` 並不代表沒有安全漏洞。邏輯錯誤、panic、OOM、演算法複雜度攻擊及資源耗盡仍可能造成安全問題。

---

## 5. 短期修復優先順序

### 第一優先：修正 Span 邊界及算術

- 將 `Span::shift` 改為受檢查運算；
- 將 `Span::new` 改為無法建立非法狀態的 API；
- 增加以下邊界測試：
  - `0`
  - `u32::MAX`
  - 負偏移；
  - 正溢位；
  - 空 Span；
  - `start == end`
  - `start > end`

### 第二優先：強化 UTF-8 處理

- 明確定義輸入是 UTF-8 字串還是任意位元組；
- 保證 token Span 落在合法字元邊界；
- 測試：
  - 中文；
  - emoji；
  - 組合字元；
  - 非法 UTF-8；
  - 截斷的多位元組序列。

### 第三優先：限制遞迴及資源消耗

- 將可迭代處理的遞迴路徑改寫為迴圈；
- 為節點數、錯誤數及輸入大小設定上限；
- 增加極深巢狀及極大輸入測試。

### 第四優先：改善集合配置

- 測量 `Node::children` 的實際分布；
- 評估 `SmallVec`；
- 檢查 `attach_leaf` 是否存在重複複製；
- 對病態輸入執行 benchmark。

### 第五優先：強制 CI 品質閘門

加入：

- rustfmt；
- Clippy；
- 全 feature 測試；
- `cargo-audit`；
- `cargo-deny`；
- fuzz smoke test；
- 必要時加入 Miri 與 sanitizer。

---

## 6. 中長期改善建議

### 6.1 拆分 crate

將 AST、解析器、證明基礎設施、匯出工具及 CLI 分離，可改善：

- 編譯速度；
- 相依邊界；
- API 穩定性；
- 子系統測試；
- feature 管理。

### 6.2 建立 `no_std` 核心

如果有嵌入式、WASM 或驗證環境需求，可將純資料結構及核心邏輯抽離成 `no_std` crate。

但應在確認實際需求後再執行，避免增加不必要的抽象及維護成本。

### 6.3 評估語法產生器

可評估：

- LALRPOP；
- Pest；
- Chumsky；
- Tree-sitter。

這些工具可能提供：

- 更明確的文法定義；
- 較一致的錯誤處理；
- 語法維護工具；
- 編輯器整合。

但手寫解析器通常在錯誤恢復、效能及 Span 控制方面更靈活，因此不建議在沒有量化收益的情況下直接全面重寫。

### 6.4 評估索引型別

可評估是否將 `u32` 改為：

- `usize`；
- `u64`；
- 受封裝的 `ByteOffset` 新型別。

例如：

```rust
pub struct ByteOffset(u32);
```

這可避免不同索引單位被錯誤混用。

是否需要改為 `u64`，應根據最大輸入尺寸及記憶體模型決定；對一般來源檔案而言，`u32` 可能已足夠。

### 6.5 使用零複製來源儲存

可評估：

```rust
Arc<str>
Arc<[u8]>
```

讓多個語法樹及診斷資訊共享來源資料。

如果輸入必須是有效 UTF-8，優先考慮 `Arc<str>`；如果需要接受任意位元組，則使用 `Arc<[u8]>`。

### 6.6 擴充屬性測試

將現有測試擴展為 `proptest` 策略，驗證：

- token Span 不重疊；
- token 完整覆蓋輸入；
- 所有 Span 均在輸入範圍內；
- UTF-8 Span 位於字元邊界；
- 語法樹沒有孤立節點；
- 節點子區間包含於父區間；
- 錯誤恢復必定向前移動；
- 解析器不因任意輸入 panic；
- 深度及資源限制始終生效。

---

## 7. 總結

此專案整體展現了良好的 Rust 開發習慣，並具備相當完整的自我驗證及測試思路。主要優點包括：

- 以安全 Rust 為主；
- 核心資料結構簡潔；
- 對解析不變條件有明確描述；
- 已考慮遞迴深度限制；
- 具備屬性測試基礎。

目前最值得優先處理的問題為：

1. `Span` 算術缺少溢位檢查；
2. Release 模式可能建立無效 Span；
3. UTF-8 字元邊界處理不完整；
4. 遞迴解析仍存在堆疊及拒絕服務風險；
5. 病態輸入可能造成大量向量配置；
6. 未完成模組已暴露為公開 API；
7. 模糊測試及安全檢查尚未完全納入 CI。

優先修復 Span 與 UTF-8 問題，接著強化資源限制、模糊測試及 CI 閘門，將能顯著提高此程式碼庫的正確性、安全性及可維護性。