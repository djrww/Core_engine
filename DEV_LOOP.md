# DEV_LOOP — 開發閉環章程(Development Loop Charter)

> **一句話**:難產的解藥不是做更多,是「**一次一件、做完關門、證據入冊**」。
> 本章程由 `cargo run --bin dev_loop` 機核裁判(CI 強制,違規即紅);看板 `BACKLOG.md` 是唯一真相。

---

## 1 · 角色與分工(三隊一裁判)

| 角色 | 誰 | 職責 |
|------|----|------|
| **產品負責人** | 用戶 | 定優先級、驗收、解凍新功能 |
| **執行者** | agent | 一站到底:計畫 → 碼 → 驗證 → 看板 → commit |
| **看板** | `BACKLOG.md` | 單一真相;口頭承諾一律不算數 |
| **裁判** | `bin dev_loop` | CI 每輪檢查看板不變式,違規即擋 |

**三隊(track)**——功能歸類對應 FEATURE_ATLAS 表3:

| 隊 | 轄區 | Atlas 類別 |
|----|------|-----------|
| 語法語義 | 載體前置、巨集、借用、重寫合流、演算法基建 | 類一/二/三/四/七 |
| 證明證書 | 證書載體、引理、外部證明器、實用載體 | 類五/六 |
| 品質基建 | 測試、差分、診斷、流水線、運維 | 類八 |

## 2 · 循環七步(每個工作時段 = 一個循環)

```
① 挑一件      —— 看板頂端最高優先項,WIP 空位才可開工
② 樹/計畫定案 —— 規則樹先在計畫層定案(既有紀律);無樹者寫 3 行方案
③ 實作        —— 碼 + 測試同時落
④ 全量門禁    —— fmt ✓ → clippy -D warnings 0 ✓ → test 全綠 ✓ → verify_all/macro_lab 相關門 ✓
⑤ CI 綠       —— 推分支,監督至全綠
⑥ 關門入冊    —— 看板狀態→done + 證據;FEATURE_ATLAS/文檔同步登記
⑦ commit      —— 提交訊息引用 DL 編號
```

## 3 · 狀態機

```
proposed → planned → building → verifying → done
    ↘ parked( 凍結/降級,須註明原因 )
```

## 4 · 硬規則(裁判強制 + 章程約束)

| 規則 | 上限 | 強制方式 |
|------|------|----------|
| **WIP 上限** | building+verifying ≤ **2** | dev_loop 機核(CI 擋) |
| **done 必有證據** | 證據欄非空(commit/CI run/門禁輸出) | dev_loop 機核(CI 擋) |
| **進行項必有驗收門** | planned/building/verifying 欄非空 | dev_loop 機核(CI 擋) |
| **proposed 堆積上限** | ≤ **5**(防止看板變倉庫) | dev_loop 機核(CI 擋) |
| **凍結規則** | DL-001…005 清空前**不開新功能**(阻塞修復/文檔除外) | 章程(產品負責人解凍) |
| **停滯條款** | 同狀態停 ≥2 循環 ⇒ 拆細或 parked | 章程(執行者提案) |
| **完成優先** | 每循環「完成數 ≥ 開工數」 | 章程 |

## 5 · 與既有門禁的關係

閉環是**開發流程層**的門禁(管「做咩、幾時算做完」);底下疊加既有**品質階梯**(fmt → clippy → test → verify_all 10 門禁 → CI 15 步全綠)。兩層都綠,一件才算關門。

## 6 · 冷啟動登記

- 本章程 + 看板 + 裁判 = **DL-000**(已 done,證據見看板)。
- 五項技術債(FEATURE_ATLAS 附錄 D-1…D-5)已排入 **DL-001…DL-005**,執行順序即編號順序:**先測試債(保護重構)→ 快贏(unwrap 訊息化)→ 門檻上調 → 下沉重構 → Rocq 攻堅**。
